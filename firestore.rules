rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Читают все, пишут только авторизованные анонимно (клиент проверяет админа по TG ID)
    match /products/{id} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}